version: "3.8"

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - MONGODB_URI=mongodb+srv://webxdeveloper:webx1234@cluster0.mjrweqv.mongodb.net/pharma_erp
      - JWT_SECRET=kapil_super_secret_key
      - JWT_EXPIRES_IN=7d
      - ACCESS_TOKEN_SECRET=your_access_secret
      - ACCESS_TOKEN_EXPIRY=15m
      - REFRESH_TOKEN_SECRET=your_refresh_secret
      - REFRESH_TOKEN_EXPIRY=7d
    restart: unless-stopped
